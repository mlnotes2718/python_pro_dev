# .pre-commit-config.yaml
repos:
  # Ruff - remote is fine, auto-discovers pyproject.toml
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.9.10
    hooks:
      - id: ruff
        args: [--fix]
        stages: [pre-commit]
      - id: ruff-format
        stages: [pre-commit]

  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.18.2
    hooks:
      - id: gitleaks
        args: ["--redact", "--report-format=csv", "--report-path=gitleaks_report.csv"]
        stages: [pre-commit, pre-push]

  # mypy and pytest as local - need your venv
  - repo: local
    hooks:
      - id: mypy
        name: mypy
        #entry: uv run mypy src
        entry: just typecheck
        language: system
        types: [python]
        pass_filenames: false
        stages: [pre-commit]

      - id: pytest
        name: pytest
        #entry: uv run pytest --cov --cov-report=term-missing
        entry: just test 
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-push]    # ← only on push